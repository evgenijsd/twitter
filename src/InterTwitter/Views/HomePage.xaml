<?xml version="1.0" encoding="utf-8" ?>
<local:BaseContentPage x:Class="InterTwitter.Views.HomePage"
                       xmlns="http://xamarin.com/schemas/2014/forms"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:local="clr-namespace:InterTwitter.Views"
                       xmlns:prism="http://prismlibrary.com"
                       prism:ViewModelLocator.AutowireViewModel="True"
                       xmlns:behaviors="clr-namespace:InterTwitter.Behaviors"
                       IconImageSource="{Binding IconPath}">

    <ContentPage.Behaviors>
        <behaviors:TabPageBehavior/>
    </ContentPage.Behaviors>

    <ContentPage.Content>

        <StackLayout>

            <Button Command="{Binding OpenFlyoutCommandAsync}"
                    Text="goto flyout"
                    VerticalOptions="Start" />
            <Label HorizontalOptions="CenterAndExpand"
                   Text="Welcome to Home!"
                   VerticalOptions="CenterAndExpand" />

        </StackLayout>

    </ContentPage.Content>

</local:BaseContentPage>