<?xml version="1.0" encoding="utf-8" ?>
<local:BaseContentPage
    x:Class="InterTwitter.Views.ProfilePage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:cards="clr-namespace:PanCardView;assembly=PanCardView"
    xmlns:cardsControls="clr-namespace:PanCardView.Controls;assembly=PanCardView"
    xmlns:ffimage="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    xmlns:fftranform ="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations"
    xmlns:local="clr-namespace:InterTwitter.Views">



    <Grid RowDefinitions="0.278*, 0.198*, 0.524*" RowSpacing="0">

        <Image
            Grid.Row="0"
            Aspect="AspectFill"
            Source="https://picsum.photos/500/500?image=122" />

        <cardsControls:CircleFrame
            Grid.Row="0"
            Margin="16,16,0,0"
            BackgroundColor="#9902060E"
            HeightRequest="32"
            HorizontalOptions="Start"
            VerticalOptions="Start"
            WidthRequest="32">
            <Image Scale="0.8" Source="ic_left_white" />
        </cardsControls:CircleFrame>

        <Grid Grid.Row="1" BackgroundColor="BlanchedAlmond">
            <StackLayout
                Grid.Column="0"
                HorizontalOptions="Start"
                VerticalOptions="Center">
                <Label Text="Jaylon Kenter" />
                <Label Text="JKenter@gmail.com" />
            </StackLayout>
            <Frame
                Grid.Column="1"
                CornerRadius="23"
                HeightRequest="12"
                HorizontalOptions="Center"
                VerticalOptions="Start">

                <Label Text="Change Profile" />
            </Frame>

        </Grid>

        <cardsControls:CircleFrame
            Grid.Row="0"
            Grid.RowSpan="2"
            Margin="16,68,0,0"
            BackgroundColor="White"
            HeightRequest="69"
            HorizontalOptions="Start"
            VerticalOptions="Center"
            WidthRequest="69">
            <cardsControls:CircleFrame
                HorizontalOptions="Center"
                Size="65"
                VerticalOptions="Center">
                <ffimage:CachedImage Aspect="AspectFill" Source="https://picsum.photos/500/500?image=290" >
                    <ffimage:CachedImage.Transformations>
                        <fftranform:CircleTransformation/>
                    </ffimage:CachedImage.Transformations>
                </ffimage:CachedImage>
            </cardsControls:CircleFrame>
            
            <!--<Image Source="https://picsum.photos/500/500?image=290" Aspect="Fill" />-->
        </cardsControls:CircleFrame>

        <StackLayout Grid.Row="2" BackgroundColor="{DynamicResource appcolor_i2}">
            <ContentView HeightRequest="40" HorizontalOptions="FillAndExpand">
                <cardsControls:TabsControl BindingContext="{x:Reference list}" StripeColor="{DynamicResource appcolor_i1}">
                    <cardsControls:TabsControl.ItemTemplate>
                        <DataTemplate>
                            <Label
                                Margin="16,0,0,0"
                                HorizontalOptions="Start"
                                Style="{DynamicResource Content_i7}"
                                Text="{Binding Title}"
                                TextColor="{DynamicResource appcolor_i4}"
                                VerticalOptions="CenterAndExpand"
                                VerticalTextAlignment="Center" />

                        </DataTemplate>
                    </cardsControls:TabsControl.ItemTemplate>
                </cardsControls:TabsControl>
            </ContentView>
            <cards:CarouselView x:Name="list" ItemsSource="{Binding MenuItems}">

                <cards:CarouselView.ItemTemplate>
                    <DataTemplate>
                        <ContentView>

                            <Label
                                HorizontalOptions="Center"
                                Text="{Binding Title}"
                                VerticalOptions="Center" />
                        </ContentView>
                    </DataTemplate>
                </cards:CarouselView.ItemTemplate>

            </cards:CarouselView>
        </StackLayout>

    </Grid>

</local:BaseContentPage>